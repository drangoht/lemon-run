# Lemon Run

A three-lane endless runner seen from behind: you run, you dodge, you swallow fruit to buy back
the lead separating you from a pursuer. Design: [docs/GDD.md](docs/GDD.md).

Unity 6000.5.6f1 - C# - URP 3D - Input System - published on
[itch.io](https://Drangoht.itch.io/lemon-run).

> This repo was born from the `unity-game-template` template. The `.claude/` files,
> `docs/PITFALLS_UNITY.md` and `tools/` come from it: they carry the experience of previous
> projects. **Fix them here when the project proves them wrong**, and push back to the template
> whatever is generalisable.

## Getting started

```powershell
# Build, launch, capture -- without ever opening the editor
& "tools/build.ps1" -Run
```

That is all: the first build **imports the project itself** (it generates `Library/` and
`ProjectSettings/` -- reckon on ~20 min), so there is nothing to open in Unity Hub beforehand.

`build.ps1` finds Unity on its own among the Unity Hub installations, then keeps its path in
`tools/local.settings.json` (not versioned). If it cannot find it -- Unity installed on another
drive, for instance -- teach it once:

```powershell
& "tools/configure.ps1" -UnityPath "<unity-folder>\6000.5.6f1\Editor\Unity.exe"
```

## Structure

```
Assets/
  Editor/            BuildTools - RenderPipelineSetup - SceneBuilder
  Scripts/Core/      BuildInfo and foundation
  Scripts/Rules/     WARNING: PURE testable logic -- no `using UnityEngine`
  Scripts/Gameplay/  MonoBehaviours
  Scripts/UI/        Screens, HUD
  Settings/          URP assets (pipeline, renderer) -- WARNING: still the template 2D renderer,
                     to be switched to 3D (GDD section 7)
  Scenes/Game.unity  WARNING: ARTEFACT regenerated by SceneBuilder
  WebGLTemplates/    Host page of the web build
tests/               xUnit on Assets/Scripts/Rules/ -- no engine required
tools/               build.ps1 - configure.ps1 - environment.ps1 - release_itch.ps1
                     serve_web.py - drive_game.py
docs/                GDD (+ gdd/) - PITFALLS_UNITY (+ pitfalls/) - RELEASE - DEVLOG - TEST_REPORT
.claude/             9 agents - 4 skills - 2 hooks
```

## The three structuring choices

**1. The scene is an artefact, not a source.** `Assets/Scenes/Game.unity` is regenerated by
`SceneBuilder.Build()` at every build. You add an object to the game **by writing code**, not by
clicking in the editor. In exchange, the whole game can be driven from the command line -- build,
launch, inject keys, capture -- and therefore checked without a human hand.
WARNING: corollary -- the scene comes out modified after every build. Discard it
(`git checkout --`) unless `SceneBuilder.cs` has changed.

**2. Every numeric rule lives in `Assets/Scripts/Rules/`**, in a static class with no engine
dependency, with its tests. `dotnet test tests/LemonRun.Tests.csproj` runs in a few seconds,
without Unity -- and a hook replays them after every edit.

**3. The binary carries its identity.** Every build writes `build_stamp.json` and a
`v<version>-<sha>` stamp displayed at the bottom right. Neither a file's date (incremental build) nor
the Windows metadata (which describes the engine) says which version you are looking at. This stamp
does.

## Useful commands

| Goal | Command |
|---|---|
| Windows build | `& "tools/build.ps1"` |
| Web build | `& "tools/build.ps1" -Target web` |
| Build + launch + capture | `& "tools/build.ps1" -Run -Capture docs/x.png` |
| Tests | `dotnet test tests/LemonRun.Tests.csproj` |
| Launch and capture | `py tools/drive_game.py --launch --capture docs/x.png` |
| Serve the web build | `py tools/serve_web.py` (WARNING: **no cache** -- see PITFALLS) |
| Publish | `& "tools/release_itch.ps1" -Version X.Y.Z -DryRun` |
| Say where Unity and Python are | `& "tools/configure.ps1" -UnityPath "..."` |

WARNING: **the Unity editor must be closed** for any command-line build -- `build.ps1` refuses to
start otherwise.
WARNING: only call `Unity.exe` by hand if you know why: its path changes from one machine to the
next, and launched with `&` in PowerShell it returns **immediately without doing anything**
(`Start-Process -Wait`). `build.ps1` handles both.

## Working with Claude Code

9 agents and 4 skills ship with the project -- see **`TEAM-GUIDE.md`**.

The three habits that matter:
- **`/project-map`** before exploring the code;
- **`docs/pitfalls/<domain>.md`** before coding in that domain (index: `docs/PITFALLS_UNITY.md`);
- **`/verify-in-game`** before writing "that should work".

## Publishing

`/publish-itch`, or the `release-manager` agent to do it end to end, devlog included.
Runbook: `docs/RELEASE.md`.

## Licences

<!-- Fonts: SIL OFL (include OFL.txt next to the .ttf). Sounds: CC0 / Kenney.
     Music: check commercial usage. Detail in docs/CREDITS.md. -->
