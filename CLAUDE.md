# CLAUDE.md -- project memory

Loaded automatically at every session: **stay short and stable**. The detail lives in files loaded
**on demand**, pointed to below. Ceiling: ~80 lines.

## The project

"Lemon Run" -- three-lane endless runner seen from behind: dodge, and swallow fruit to buy back the
lead on a pursuer (`docs/GDD.md`).
**Engine: Unity 6000.5.6f1** (C#, URP 3D, Input System package). Published on
`https://Drangoht.itch.io/lemon-run`.

## Where to read what -- and nothing else

| Need | Open | Warning |
|---|---|---|
| Design intent | `docs/GDD.md` (contents) then **the one** `docs/gdd/<system>.md` concerned | do not read the other systems |
| Fill in a GDD still full of gaps | **`/write-the-gdd`** skill | |
| Locate code | **`/project-map`** skill | rather than Glob/Grep from cold |
| Pitfall of a domain | `docs/PITFALLS_UNITY.md` = **index** -> 2-3 `docs/pitfalls/*.md` files | never all twelve |
| Check that a change works | **`/verify-in-game`** skill | |
| Publish | **`/publish-itch`** skill, or the `release-manager` agent | |

**Before coding in a domain, read its pitfalls file; every new pitfall goes back into it, in the
same commit.** That file is what stops a bug from happening again six months later.

## Current phase

**Phase 0 -- setting up** (started on 2026-09-19) -- done, bar the draft page going public

- [x] First build done on 2026-09-19: `& "tools/build.ps1" -Run` builds, launches and captures.
- [x] `docs/GDD.md`: pitch, loop, controls settled on 2026-09-19; 4 to 6 written system by system.
- [x] Pipeline switched to **URP 3D** on 2026-09-19: perspective camera, sun, 2D renderer gone.
- [x] itch page created and **0.1.0 published** on the `html5` channel (2026-09-19), played in
      the browser and checked there. The page is still a **Draft**.

## Agent team -- delegate, but not at a loss

The nine agents in `.claude/agents/` are already listed with their description at the top of the
session -- do not re-describe them here. Who does what **and in what order**: **`TEAM-GUIDE.md`**.

WARNING: **an agent starts cold**: it re-reads CLAUDE.md, the GDD, the map, the pitfalls -- about
8,000 tokens before its first action, quite possibly the same documents you have just read yourself.
Delegate when the task is **in its speciality** *and* **big enough to amortise that** (designing a
system, a test pass, a release). A ten-line fix, a question, a read: do it yourself. When you do
delegate, **pass in the instruction what you already know** (files concerned, decision taken,
pitfall identified) rather than making it rediscover them.

WARNING: **an agent describing a stale state of the project is worse than no agent at all**: it
gives wrong instructions with authority. When a phase ends, re-read the agents it concerns.

## Conventions

- **Build**: `& "tools/build.ps1"` -> `Build\Windows\LemonRun.exe`; `-Target web` ->
  `Build\Web\`; `-Run` chains launch and capture. WARNING: the Unity editor must be **closed**.
- **Publishing**: `tools/release_itch.ps1 -Version X.Y.Z` (try `-DryRun` first). The script sets
  `bundleVersion` itself -- **do not edit it by hand**.
- **The scene is an ARTEFACT**: `Assets/Scenes/Game.unity` is regenerated by `SceneBuilder.Build()`
  at every build. You add an object to the game **by writing code**, not by clicking in the editor.
  It comes out modified after every build -- discard it (`git checkout --`) unless `SceneBuilder.cs`
  has changed.
- **Pure testable logic**: `Assets/Scripts/Rules/` -- static classes **with no engine dependency**.
  The `MonoBehaviour`s delegate to them. Tests: `dotnet test tests/LemonRun.Tests.csproj` (no
  engine, no build required; replayed by a hook after every edit).
- **Nothing deprecated**: URP (never Built-in), Input System (never the old Input Manager). Report
  any deprecation warning without waiting to be asked.
- **No hard-coded tool path** (Unity, Python): `tools/environment.ps1` resolves them and remembers
  them in `tools/local.settings.json`, not versioned.
- Style: PascalCase for classes/methods, `_camelCase` for private fields, `readonly` by default.
  Comments in English, and they explain the **why**.
- Tuning must be adjustable **without recompiling** as soon as a value deserves to be tried several
  times.
- UI palette: <!-- hex codes --> - Font: <!-- family -->
- WARNING: three pitfalls that raise nothing and are met everywhere -- the detail in the file named:
  **AZERTY** keyboard (`docs/pitfalls/input.md`), `Art/` is not `Resources/`
  (`docs/pitfalls/assets-import.md`), misleading Unity exit code (`docs/pitfalls/build.md`).

## Doc maintenance

- Update **in the commit** that makes what they describe wrong: `docs/gdd/`, `/project-map`,
  `docs/pitfalls/`. A stale document gives a wrong answer with authority.
- `README.md` -- update at every phase change or major addition.
- WARNING: **every document re-read at every task has a ceiling** (this file: 80 lines; `GDD.md`,
  `/project-map`: 150; a pitfalls or system file: 150). Once reached, **split** and leave an index.
  Without that reflex, the cost per task climbs until it exceeds the task.
